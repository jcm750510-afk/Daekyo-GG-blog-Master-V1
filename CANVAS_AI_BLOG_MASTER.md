# ChatGPT AI 눈높이 블로그 마스터

## 목적
기존 React/Vite 기반 AI 눈높이 블로그 프롬프트 마스터의 핵심 규칙을 ChatGPT/GPT 환경에서 재사용하기 위한 설계 문서입니다.

## 핵심 워크플로
1. 타겟 독자, 제품, 주제, 지역/센터명, 포스팅 유형, 이미지 스타일, 원장님 요청사항을 입력한다.
2. 입력값을 분석해 후킹 제목 5개를 제안한다.
3. 사용자가 제목을 선택한다.
4. 선택 제목을 기준으로 SEO형 블로그 본문을 생성한다.
5. A.E.A 3층 구조(권위-근거-행동)와 교육정보 90 : 홍보 10 비율을 적용한다.
6. 학부모 공감 도입, 의심 사전 차단, 로컬 센터 연결, 진단/상담 CTA를 자연스럽게 반영한다.
7. 본문에 적절한 시각화 위치와 이미지 생성 프롬프트를 삽입한다.
8. 최종 결과는 블로그에 바로 활용할 수 있도록 정리한다.

## 구현 방향
- 현재 앱의 Gemini 브라우저 API 키 의존 방식은 그대로 확장하지 않는다.
- ChatGPT 환경에서는 모델 자체를 생성 엔진으로 사용하고, GitHub 저장소의 constants.ts에 정의된 지식베이스·시스템 지침·이미지 스타일을 규칙 원본으로 취급한다.
- API 키/비밀값은 저장소에 커밋하지 않는다.
- React UI는 향후 GPT/웹앱 인터페이스에 맞춰 재사용할 수 있도록 프레젠테이션 계층과 생성 로직을 분리한다.

## 기존 앱과의 연계
- `src/constants.ts`: 지식베이스, 제목 생성 지침, 본문 생성 지침, 이미지 스타일의 원본.
- `src/App.tsx`: 입력 → 제목 생성 → 제목 선택 → 본문 생성 → 복사 UX의 원본 구현.

## 이미지 스타일
1. 에듀케이션 일러스트
2. 플랫 디자인
3. 라인 아트 / 아이콘형
4. 2.5D 이소메트릭
5. 인포그래픽 차트형
6. 핸드 스케치 / 낙서풍
7. 3D 클레이 스타일

## 주의
이 문서는 현재 저장소의 원본 코드를 보존하면서 ChatGPT 기반 구현으로 전환하기 위한 기준 문서입니다. 실제 ChatGPT 내부 UI 구현 방식은 해당 제품의 지원 범위에 맞춰 별도로 구성해야 합니다.
